<?xml version="1.0" encoding="utf-8"?>
<Project ToolsVersion="15.0" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
	<!-- This file is run automatically when the project is built. https://docs.microsoft.com/en-us/visualstudio/msbuild/customize-your-build?view=vs-2019 
		 -->
	<Target Name="GovUkFrontend_CheckGovUkFrontendPackage" BeforeTargets="BeforeBuild">
		<PropertyGroup>
			<GovUkPackageFolder>$(MsBuildThisFileDirectory)..\node_modules\govuk-frontend</GovUkPackageFolder>
		</PropertyGroup>

		<Exec Command="where node" Condition="!Exists('$(GovUkPackageFolder)')" ConsoleToMsBuild="true" IgnoreExitCode="true">
			<Output TaskParameter="ConsoleOutput" PropertyName="CheckForNode" />
		</Exec>
		<Error Text="Node.js is not installed. Node.js is required to install the govuk-frontend package which contains the conformance tests. See https://nodejs.org" Condition="$(CheckForNode.Contains('Could not find files'))" />
		<Exec Command="where yarn" Condition="!Exists('$(GovUkPackageFolder)')" ConsoleToMsBuild="true" IgnoreExitCode="true">
			<Output TaskParameter="ConsoleOutput" PropertyName="CheckForYarn" />
		</Exec>
		<Error Text="Yarn Classic is required to install the govuk-frontend package which contains the conformance tests. Run 'npm install --global yarn'. If you get an error UNABLE_TO_GET_ISSUER_CERT_LOCALLY you need to click the padlock next to any site in the address bar of your browser, and download the CA certificate for your network in .PEM format. Then set the environment variable NODE_EXTRA_CA_CERTS to the path to that certificate, and restart Visual Studio." Condition="$(CheckForYarn.Contains('Could not find files'))" />
		<Message Text="GOV.UK Frontend NPM package not found. Running 'yarn install'." Condition="!Exists('$(GovUkPackageFolder)')" />
		<Exec Command="yarn install" WorkingDirectory="$(MsBuildThisFileDirectory).." Condition="!Exists('$(GovUkPackageFolder)')" />
	</Target>
</Project>
