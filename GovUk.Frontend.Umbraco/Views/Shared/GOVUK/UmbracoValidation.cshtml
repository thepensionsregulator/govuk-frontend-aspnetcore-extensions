@using Microsoft.AspNetCore.Hosting
@using Microsoft.Extensions.Hosting
@* @inject Smidge.SmidgeHelper smidgeHelper *@
@inject IWebHostEnvironment environment
@{
    var urls = new string[0];// await smidgeHelper.GenerateJsUrlsAsync("govuk-frontend-validation", environment.EnvironmentName.Equals(Environments.Development, StringComparison.OrdinalIgnoreCase));
}
@foreach (var url in urls)
{
    <script src="@url" type="module"></script>
}