@model AddressLookupViewModel
@inject ITprAddressLookupEndpointUrlProvider urlProvider
@inject ITprCountryRepository countryRepository

<h2 class="govuk-heading-m">Shipping address</h2>
<p class="govuk-body">Tell us where you would like your order shipped to.</p>

<tpr-address-lookup role="Primary"
                    data-address-lookup-search-url="@urlProvider.GetAddressLookupSearchEndpoint()"
                    data-address-lookup-id-url="@urlProvider.GetAddressLookupIdEndpoint()"
                    data-address-lookup-postcode-label="Postcode"
                    data-address-lookup-postcode-required="Enter a postcode"
                    data-address-lookup-postcode-maxlength="Postcode must be 20 characters or less"
                    data-address-lookup-postcode-pattern="Enter a valid UK postcode"
                    data-address-lookup-county-label="County (optional)"
                    data-address-lookup-county-maxlength="County must be 100 characters or less"
                    data-address-lookup-building-label="Building or house number (optional)"
                    data-address-lookup-building-maxlength="Building or house number must be 100 characters or less"
                    data-address-lookup-select-address-label="Select address"
                    data-address-lookup-select-address-required="Select an address">
    <tpr-address-lookup-legend>Shipping address</tpr-address-lookup-legend>

    <govuk-client-side-validation error-message-required="Enter address line 1"
                                  error-message-maxlength="Address line 1 must be 200 characters or less">
        <govuk-input for="ShippingAddressLine1" autocomplete="address-line1"
                     input-attributes="@(new Dictionary<string, string?>{{"data-address-lookup", "address-line-1"}})">
            <govuk-input-label>Address line 1</govuk-input-label>
            @if (ViewContext.ModelState.ContainsKey("ShippingAddressLine1") && ViewContext.ModelState["ShippingAddressLine1"]!.Errors.Count > 0)
            {
                <govuk-input-error-message>@ViewContext.ModelState["ShippingAddressLine1"]!.Errors[0].ErrorMessage</govuk-input-error-message>
            }
        </govuk-input>
    </govuk-client-side-validation>

    <govuk-client-side-validation error-message-maxlength="Address line 2 must be 200 characters or less">
        <govuk-input for="ShippingAddressLine2" autocomplete="address-line2"
                     input-attributes="@(new Dictionary<string, string?>{{"data-address-lookup", "address-line-2"}})">
            <govuk-input-label>Address line 2 (optional)</govuk-input-label>
            @if (ViewContext.ModelState.ContainsKey("ShippingAddressLine2") && ViewContext.ModelState["ShippingAddressLine2"]!.Errors.Count > 0)
            {
                <govuk-input-error-message>@ViewContext.ModelState["ShippingAddressLine2"]!.Errors[0].ErrorMessage</govuk-input-error-message>
            }
        </govuk-input>
    </govuk-client-side-validation>

    <govuk-client-side-validation error-message-required="Enter a town or city"
                                  error-message-maxlength="Town or city must be 100 characters or less">
        <govuk-input for="ShippingTownOrCity" class="govuk-input--width-20" autocomplete="address-level2"
                     input-attributes="@(new Dictionary<string, string?>{{"data-address-lookup", "town-or-city"}})">
            <govuk-input-label>Town or city</govuk-input-label>
            @if (ViewContext.ModelState.ContainsKey("ShippingTownOrCity") && ViewContext.ModelState["ShippingTownOrCity"]!.Errors.Count > 0)
            {
                <govuk-input-error-message>@ViewContext.ModelState["ShippingTownOrCity"]!.Errors[0].ErrorMessage</govuk-input-error-message>
            }
        </govuk-input>
    </govuk-client-side-validation>

    <govuk-client-side-validation error-message-maxlength="Province, region or state must be 100 characters or less">
        <govuk-input for="ShippingCounty" class="govuk-input--width-20" autocomplete="address-level3"
                     input-attributes="@(new Dictionary<string, string?>{{"data-address-lookup", "region-international"}})">
            <govuk-input-label>Province/ region/ state (optional)</govuk-input-label>
            @if (ViewContext.ModelState.ContainsKey("ShippingCounty") && ViewContext.ModelState["ShippingCounty"]!.Errors.Count > 0)
            {
                <govuk-input-error-message>@ViewContext.ModelState["ShippingCounty"]!.Errors[0].ErrorMessage</govuk-input-error-message>
            }
        </govuk-input>
    </govuk-client-side-validation>

    <govuk-client-side-validation error-message-required="Select a country">
        <govuk-select for="ShippingCountry" select-class="govuk-input--width-20" select-autocomplete="country"
                     select-data-address-lookup="country">
            <govuk-select-label>Country</govuk-select-label>
            @if (ViewContext.ModelState.ContainsKey("ShippingCountry") && ViewContext.ModelState["ShippingCountry"]!.Errors.Count > 0)
            {
                <govuk-select-error-message>@ViewContext.ModelState["ShippingCountry"]!.Errors[0].ErrorMessage</govuk-select-error-message>
            }
            @{
                var countries = countryRepository.GetCountries();
                var selectEmpty = !countries.ContainsKey(ViewContext.ModelState["ShippingCountry"]?.AttemptedValue?.ToString() ?? string.Empty);
                <govuk-select-item selected="@(selectEmpty)"></govuk-select-item>
                foreach (var country in countries)
                {
                    var selected = country.Key.ToString() == ViewContext.ModelState["ShippingCountry"]?.AttemptedValue;
                    <govuk-select-item value="@country.Value" selected="@(selected)">@country.Key</govuk-select-item>
                }
            }
        </govuk-select>
    </govuk-client-side-validation>

    <govuk-client-side-validation error-message-required="Enter a postal code or zip code"
                                  error-message-maxlength="Postal code or zip code must be 20 characters or less">
        <govuk-input for="ShippingPostcode" class="govuk-input--width-10" autocomplete="postal-code"
                     input-attributes="@(new Dictionary<string, string?>{{"data-address-lookup", "postcode-international"}})">
            <govuk-input-label>Postal code/ zip code</govuk-input-label>
            @if (ViewContext.ModelState.ContainsKey("ShippingPostcode") && ViewContext.ModelState["ShippingPostcode"]!.Errors.Count > 0)
            {
                <govuk-input-error-message>@ViewContext.ModelState["ShippingPostcode"]!.Errors[0].ErrorMessage</govuk-input-error-message>
            }
        </govuk-input>
    </govuk-client-side-validation>

    <input type="hidden" id="ShippingUPRN" name="ShippingUPRN" value="@Model?.ShippingUPRN" data-address-lookup="UPRN" />
    <input type="hidden" id="ShippingCountryCode" name="ShippingCountryCode" value="@Model?.ShippingCountryCode" data-address-lookup="country-code" />
</tpr-address-lookup>

<h2 class="govuk-heading-m">Billing address</h2>
<p class="govuk-body">Tell us where we should bill your order to.</p>

<tpr-address-lookup role="Secondary"
                    same-as-primary-checkbox-label="Same as shipping address"
                    data-address-lookup-search-url="@urlProvider.GetAddressLookupSearchEndpoint()"
                    data-address-lookup-id-url="@urlProvider.GetAddressLookupIdEndpoint()"
                    data-address-lookup-postcode-label="Postcode"
                    data-address-lookup-postcode-required="Enter a postcode"
                    data-address-lookup-postcode-maxlength="Postcode must be 20 characters or less"
                    data-address-lookup-postcode-pattern="Enter a valid UK postcode"
                    data-address-lookup-county-label="County (optional)"
                    data-address-lookup-county-maxlength="County must be 100 characters or less"
                    data-address-lookup-building-label="Building or house number (optional)"
                    data-address-lookup-building-maxlength="Building or house number must be 100 characters or less"
                    data-address-lookup-select-address-label="Select address"
                    data-address-lookup-select-address-required="Select an address">

    <tpr-address-lookup-legend>Billing address</tpr-address-lookup-legend>

    <govuk-client-side-validation error-message-required="Enter address line 1"
                                  error-message-maxlength="Address line 1 must be 200 characters or less">
        <govuk-input for="BillingAddressLine1" autocomplete="address-line1"
                     input-attributes="@(new Dictionary<string, string?>{{"data-address-lookup", "address-line-1"}})">
            <govuk-input-label>Address line 1</govuk-input-label>
            @if (ViewContext.ModelState.ContainsKey("BillingAddressLine1") && ViewContext.ModelState["BillingAddressLine1"]!.Errors.Count > 0)
            {
                <govuk-input-error-message>@ViewContext.ModelState["BillingAddressLine1"]!.Errors[0].ErrorMessage</govuk-input-error-message>
            }
        </govuk-input>
    </govuk-client-side-validation>

    <govuk-client-side-validation error-message-maxlength="Address line 2 must be 200 characters or less">
        <govuk-input for="BillingAddressLine2" autocomplete="address-line2"
                     input-attributes="@(new Dictionary<string, string?>{{"data-address-lookup", "address-line-2"}})">
            <govuk-input-label>Address line 2 (optional)</govuk-input-label>
            @if (ViewContext.ModelState.ContainsKey("BillingAddressLine2") && ViewContext.ModelState["BillingAddressLine2"]!.Errors.Count > 0)
            {
                <govuk-input-error-message>@ViewContext.ModelState["BillingAddressLine2"]!.Errors[0].ErrorMessage</govuk-input-error-message>
            }
        </govuk-input>
    </govuk-client-side-validation>

    <govuk-client-side-validation error-message-required="Enter a town or city"
                                  error-message-maxlength="Town or city must be 100 characters or less">
        <govuk-input for="BillingTownOrCity" class="govuk-input--width-20" autocomplete="address-level2"
                     input-attributes="@(new Dictionary<string, string?>{{"data-address-lookup", "town-or-city"}})">
            <govuk-input-label>Town or city</govuk-input-label>
            @if (ViewContext.ModelState.ContainsKey("BillingTownOrCity") && ViewContext.ModelState["BillingTownOrCity"]!.Errors.Count > 0)
            {
                <govuk-input-error-message>@ViewContext.ModelState["BillingTownOrCity"]!.Errors[0].ErrorMessage</govuk-input-error-message>
            }
        </govuk-input>
    </govuk-client-side-validation>

    <govuk-client-side-validation error-message-maxlength="Province, region or state must be 100 characters or less">
        <govuk-input for="BillingCounty" class="govuk-input--width-20" autocomplete="address-level3"
                     input-attributes="@(new Dictionary<string, string?>{{"data-address-lookup", "region-international"}})">
            <govuk-input-label>Province/ region/ state (optional)</govuk-input-label>
            @if (ViewContext.ModelState.ContainsKey("BillingCounty") && ViewContext.ModelState["BillingCounty"]!.Errors.Count > 0)
            {
                <govuk-input-error-message>@ViewContext.ModelState["BillingCounty"]!.Errors[0].ErrorMessage</govuk-input-error-message>
            }
        </govuk-input>
    </govuk-client-side-validation>

    <govuk-client-side-validation error-message-required="Select a country">
        <govuk-select for="BillingCountry" select-class="govuk-input--width-20" select-autocomplete="country"
                      select-data-address-lookup="country">
            <govuk-select-label>Country</govuk-select-label>
            @if (ViewContext.ModelState.ContainsKey("BillingCountry") && ViewContext.ModelState["BillingCountry"]!.Errors.Count > 0)
            {
                <govuk-select-error-message>@ViewContext.ModelState["BillingCountry"]!.Errors[0].ErrorMessage</govuk-select-error-message>
            }
            @{
                var countries = countryRepository.GetCountries();
                var selectEmpty = !countries.ContainsKey(ViewContext.ModelState["BillingCountry"]?.AttemptedValue?.ToString() ?? string.Empty);
                <govuk-select-item selected="@(selectEmpty)"></govuk-select-item>
                foreach (var country in countries)
                {
                    var selected = country.Key.ToString() == ViewContext.ModelState["BillingCountry"]?.AttemptedValue;
                    <govuk-select-item value="@country.Value" selected="@(selected)">@country.Key</govuk-select-item>
                }
            }
        </govuk-select>
    </govuk-client-side-validation>

    <govuk-client-side-validation error-message-required="Enter a postal code or zip code"
                                  error-message-maxlength="Postal code or zip code must be 20 characters or less">
        <govuk-input for="BillingPostcode" class="govuk-input--width-10" autocomplete="postal-code"
                     input-attributes="@(new Dictionary<string, string?>{{"data-address-lookup", "postcode-international"}})">
            <govuk-input-label>Postal code/ zip code</govuk-input-label>
            @if (ViewContext.ModelState.ContainsKey("BillingPostcode") && ViewContext.ModelState["BillingPostcode"]!.Errors.Count > 0)
            {
                <govuk-input-error-message>@ViewContext.ModelState["BillingPostcode"]!.Errors[0].ErrorMessage</govuk-input-error-message>
            }
        </govuk-input>
    </govuk-client-side-validation>

    <input type="hidden" id="BillingUPRN" name="BillingUPRN" value="@Model?.BillingUPRN" data-address-lookup="UPRN" />
    <input type="hidden" id="BillingCountryCode" name="BillingCountryCode" value="@Model?.BillingCountryCode" data-address-lookup="country-code" />
</tpr-address-lookup>

<govuk-button type="submit">Save</govuk-button>